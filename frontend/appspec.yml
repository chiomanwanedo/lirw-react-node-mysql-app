version: 0.0
os: linux

files:
  - source: /dist
    destination: /var/www/html
    overwrite: yes

hooks:
  BeforeInstall:
    - location: scripts/install.sh
      runas: root
  AfterInstall:
    - location: scripts/start.sh
      runas: root
